# Dependencias principales
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
geopy>=2.2.0
timezonefinder>=6.0.0
pytz>=2021.3

# Para compilación de Sass/SCSS (crítico)
libsass>=0.22.0  # Paquete pip que proporciona 'sass'

# Herramientas de construcción y packaging
setuptools>=68.0.0  # Versión moderna para evitar errores
wheel>=0.40.0

# Streamlit (si despliegas la interfaz)
streamlit>=1.0.0

# Dependencias de Google (si usas APIs)
google-api-python-client>=2.0.0
google-auth-oauthlib>=0.40.0